apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: frontend-ticket-backendconfig
  namespace: event-hub
spec:
  healthCheck:
    checkIntervalSec: 15
    timeoutSec: 5
    healthyThreshold: 1
    unhealthyThreshold: 2
    type: HTTP
    requestPath: /healthz
    port: 3000
